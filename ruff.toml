line-length = 120
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM"]
# report lines are single f-strings; wrapping them would not make them clearer. The zips
# pair lists built side by side; a strict= on each would say nothing.
ignore = ["E501", "B905"]
