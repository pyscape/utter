[mypy]
files = scripts
strict = True
warn_unreachable = True
# numpy, kaldi_native_fbank, vosk and the utterpy binding are optional imports the scripts
# guard at runtime; of those only numpy and utterpy ship stubs.
ignore_missing_imports = True
